"""Generated protobuf stubs for the Symba wire contract.

COMMITTED and regenerated per engine tag via ``make proto-gen`` — never edited by
hand. The engine repo owns the ``.proto`` source of truth (spec 4.1). The tag the
stubs were generated from lives in the sibling ``VERSION`` file and is surfaced as
``symba.__engine_protocol__``.
"""
